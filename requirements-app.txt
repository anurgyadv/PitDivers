# PitDivers dashboard runtime dependencies (works on CPU or GPU, any OS).
#
# Install PyTorch FIRST and separately — the correct build depends on your
# machine (CPU-only vs a specific CUDA version). See QUICKSTART.md. Then:
#   pip install -r requirements-app.txt
#
# This covers the dashboard, live DA3 depth, and offline GLB reconstruction.
# It intentionally omits GPU-only/build-heavy extras (xformers, gsplat) — DA3
# falls back to a pure-PyTorch path without them. Open3D is only needed by the
# optional tools/mesh_from_glb.py meshing tool (see tools/requirements.txt).

# Web dashboard
fastapi>=0.115,<1
uvicorn>=0.30,<1

# Core numerics / imaging
numpy>=1.24,<2
opencv-python>=4.10,<5
Pillow>=10,<13
matplotlib>=3.7
scipy>=1.10

# DA3 model + inference runtime
einops>=0.7
tqdm>=4.65
omegaconf>=2.3
addict>=2.4
huggingface-hub>=0.28,<2
safetensors>=0.4,<1
requests>=2.28

# Export chain (imported when the DA3 API loads; needed for GLB reconstruction)
trimesh>=4.0
imageio>=2.31
plyfile>=1.0
pycolmap>=0.6
moviepy==1.0.3
typer>=0.9
